import { Dex } from '../hooks/useSwap';
export declare const BPS = 10000;
export declare const MAX_SLIPPAGE_IN_BIPS = 2000;
export declare const parseSlippageInput: (str: string) => number;
export declare const validateSlippageInput: (str: string) => {
    isValid: boolean;
    message?: string;
};
export declare const SlippageInput: ({ slippage, setSlippage, }: {
    slippage: number;
    setSlippage: (value: number) => void;
}) => JSX.Element;
declare function Settings({ slippage, setSlippage, deadline, setDeadline, allDexes, excludedDexes, onShowSource, }: {
    slippage: number;
    setSlippage: (value: number) => void;
    deadline: number;
    setDeadline: (value: number) => void;
    allDexes: Dex[];
    excludedDexes: Dex[];
    onShowSource: () => void;
}): JSX.Element;
export default Settings;
